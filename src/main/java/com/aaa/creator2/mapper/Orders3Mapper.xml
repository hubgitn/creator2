<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aaa.creator2.mapper.Orders3Mapper">
    <select id="findOrders3" resultType="int">
          select COUNT(*) from orders where ostateid=3;
    </select>
</mapper>