<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aaa.creator2.mapper.UpdateOSMapper">
    <update id="updateOS" parameterType="Integer">
        update orders set ostateid=4 where oid=#{oid}
    </update>
</mapper>