<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aaa.creator2.mapper.Orders1Mapper">
    <select id="findOrders1" resultType="int">
          select COUNT(*) from orders where ostateid=1;
    </select>
</mapper>